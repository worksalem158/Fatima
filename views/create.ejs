<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>زبون جديد - متجر فطيمة</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Cairo', sans-serif; background-color: #f8f9fa; }</style>
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-body p-4">
                        <h3 class="text-center mb-4 text-primary">تسجيل زبون جديد</h3>
                        <form action="/add-customer" method="POST" enctype="multipart/form-data">
                            
                            <h5 class="border-bottom pb-2 mb-3">بيانات الزبون</h5>
                            <div class="mb-3">
                                <label class="form-label">اسم الزبون</label>
                                <input type="text" name="customerName" class="form-control form-control-lg" placeholder="مثال: خالة عيشة" required>
                            </div>

                            <h5 class="border-bottom pb-2 mb-3 mt-4">بيانات المنتج الأول</h5>
                            <div class="mb-3">
                                <label class="form-label">اسم المنتج</label>
                                <input type="text" name="productName" class="form-control" placeholder="مثال: قماش كتان" required>
                            </div>
                            <div class="row">
                                <div class="col-6 mb-3">
                                    <label class="form-label">السعر (د.ج)</label>
                                    <input type="number" name="price" class="form-control" placeholder="1000" required>
                                </div>
                                <div class="col-6 mb-3">
                                    <label class="form-label">حالة الدفع</label>
                                    <select name="status" class="form-select">
                                        <option value="paid">مدفوع (خالص)</option>
                                        <option value="unpaid" selected>آجل (كريدي)</option>
                                    </select>
                                </div>
                            </div>
                            <div class="mb-4">
                                <label class="form-label">صورة المنتج</label>
                                <input type="file" name="image" class="form-control" accept="image/*">
                            </div>

                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-primary btn-lg">حفظ البيانات</button>
                                <a href="/" class="btn btn-outline-secondary">إلغاء والعودة</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
